name: PHP CI/CD

on:
  push:
    branches:
      - main # Replace with your main branch name
  pull_request:
    branches:
      - main # Replace with your main branch name

jobs:
  runs-on: ubuntu-latest
  environment: ${{ github.ref == 'refs/heads/main' && 'value_for_main_branch' || 'value_for_other_branches' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: print
        run: echo "$name"
      
        
